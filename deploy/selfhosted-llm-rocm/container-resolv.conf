# Bind-mounted into rootless Podman containers when podman-compose ignores
# Compose `dns:` or netavark/aardvark-dns does not forward external queries.
# Edit this file if your network requires a corporate/router/Tailscale DNS.
nameserver 1.1.1.1
nameserver 8.8.8.8
options timeout:2 attempts:2
